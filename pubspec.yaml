name: cityshop_admin
description: CityShop administrator app — separate from the shopper/seller APK.
publish_to: "none"
version: 1.0.17+18

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.11.0
  go_router: ^17.3.0
  provider: ^6.1.5+1
  shared_preferences: ^2.5.5
  flutter_secure_storage: ^10.3.1
  cached_network_image: ^3.4.1
  intl: ^0.20.3
  image_picker: ^1.2.3
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
